# Life OS

## Deployment

Connected to Vercel via GitHub (`dan-suen/life_os`). Pushing to `main` triggers an automatic deploy.

The app uses Supabase (PostgreSQL) for data storage. Connection credentials are configured in Vercel project settings.
